services:
  backend:
    build: ./backend
    container_name: python-backend
    ports:
      - "${WEB_PORT}:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000"]
      interval: 30s
      timeout: 10s
      retries: 3
